# Node
node_modules/
app/dist/

# Rust / Tauri
target/
app/src-tauri/target/
app/src-tauri/gen/schemas/

# Go (cli, added later)
cli/*.exe

# Agent / editor local config — never publish
.claude/
.cursor/
.vscode/
.idea/

# OS / editor
.DS_Store
*.log
Thumbs.db

# temp / scratch (screenshots written during dev)
*.tmp
.tmp-shot.png
*.tmp.png

# Go CLI build artifacts (covered by cli/*.exe above; kept explicit)
cli/scanline
cli/scanline-cli.exe
