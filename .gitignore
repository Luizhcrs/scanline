# Node
node_modules/
app/dist/
app/dist-electron/

# Electron build output
app/dist-installer/

# Go (cli, added later)
cli/*.exe

# Agent / editor local config — never publish
.claude/
.cursor/
.vscode/
.idea/

# OS / editor
.DS_Store
*.log
Thumbs.db

# temp / scratch (screenshots written during dev)
*.tmp
.tmp-shot.png
*.tmp.png

# Go CLI build artifacts (covered by cli/*.exe above; kept explicit)
cli/scanline
cli/scanline-cli.exe
# CLI build artifact bundled with Electron
app/scanline-cli
app/scanline-cli.exe

# Internal planning docs kept local, NEVER published (clean-room: the feature
# catalog maps GPL cmux source by file:line — must not ship in a public repo).
internal/

# Audit workflow scratch
WF_*.json
WF_*.md

# Internal planning/AI docs — never publish
docs/superpowers/
